<h3>Reviews</h3>

  <% @product.reviews.each do |review| %>
    <p>
      <%= review.comment %>
      <br>
      <% if review.user != nil %>
        <em> by <%= link_to review.user.name, review.user %></em>
        Added on: <%= review.created_at %>
      <% end %>
    </p>
  <% end %>